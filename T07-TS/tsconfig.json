{
  // 编译器的选项
  "compilerOptions": {
    // lib用来指定项目中要使用的库
    "lib": ["es6", "dom"],
    // module 指定要使用的模块化的规范, CommonJS, ES2015
    "module": "es2015",
    // target 用来指定把ts编译成那个版本的js
    "target": "es2015",
    // outDir 用来指定编译后文件所在的目录
    "outDir": "./dist",
    // 是否移除注释
    "removeComments": true,
    // 当有错误时不生成编译后的文件
    // "noEmitOnError": true,
    // 所有严格检查的总开关
    "strict": true,
    // 不允许隐式的any类型
    "noImplicitAny": true,
    // 不允许不明确类型的this
    "noImplicitThis": true,
    // 严格的检查空值
    "strictNullChecks": true
  },
  // 指定编译的文件路径
  "include": ["./**/*"],
  // 不需要被编译的文件目录
  "exclude": []
}
